# Keep this false when testing the modern UI against the existing Django data.
VITE_USE_MOCK_DATA=false

# Set true only for isolated frontend design work without Django running.
# VITE_USE_MOCK_DATA=true
# VITE_API_BASE_URL=
# MLADIS_DJANGO_URL=http://127.0.0.1:8000
