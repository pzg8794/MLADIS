# Admin Photo Rotation Controls

Status: TODO

Public stay cards now rotate their gallery images automatically. The public website must not show a "rotate photos" control.

When the admin settings/listing editor is redesigned, add an admin-only control for each listing:

- Enable or disable public photo rotation.
- Configure rotation interval, default 5 seconds.
- Configure image order.
- Store this as listing/stay presentation settings so public cards can reference the stay object instead of duplicating display logic.
